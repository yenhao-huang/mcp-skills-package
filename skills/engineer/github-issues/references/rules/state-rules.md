# State Rules

Before each new issue workflow, reset `STATE.md` from
`references/template/STATE.template.md` and fill in repository and issue
identifiers.

Use only these statuses: `pending`, `in_progress`, `completed`, `blocked`, and
`skipped`. Record URLs, issue numbers, API responses, or read-back fields as
evidence. A mutation is not completed until its result has been verified.
